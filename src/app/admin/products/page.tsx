import Link from "next/link";
import { Plus, Edit3 } from "lucide-react";
import { createClient } from "@/lib/supabase/server";

export const dynamic = "force-dynamic";
export default async function AdminProductsPage() { const supabase = await createClient(); const { data } = await supabase.from("products").select("id,title,slug,price,status,shops(name)").order("created_at", { ascending: false }); const products = (data ?? []) as Record<string, unknown>[]; return <div className="p-6 sm:p-8"><div className="mb-8 flex items-end justify-between"><div><p className="text-[11px] font-black uppercase tracking-[.18em] text-orange-500">Catalog control</p><h1 className="mt-2 text-3xl font-black">Products</h1></div><Link href="/admin/products/new" className="button-primary bg-orange-500"><Plus className="size-4" /> Add product</Link></div><section className="surface divide-y dark:divide-white/10">{products.length ? products.map((product) => <div key={String(product.id)} className="flex items-center justify-between gap-4 p-5"><div><h2 className="font-black">{String(product.title)}</h2><p className="text-xs text-slate-500">{String((product.shops as Record<string, unknown> | null)?.name ?? "") } · {String(product.status ?? "draft")}</p></div><Link href={`/admin/products/${String(product.id)}/edit`} className="button-secondary"><Edit3 className="size-4" /> Edit</Link></div>) : <p className="p-10 text-center text-sm text-slate-500">No products yet.</p>}</section></div>; }